import '../App.css';

const Header = () => {
	return <div className='App-header'>I ❤️ React</div>;
};

// export statements allow content to be accessed in other files
// in our project. Contrast this with the import statement, which allows content from external files to be used in the current file.
export default Header;
